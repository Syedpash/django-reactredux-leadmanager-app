It is a lead manager app
    -- User can register
    -- Can login with credentials
    -- Implemented Token based authentication
    -- User can have there own leads

Used technologies are
    Django,  Django Rest Framework, React, Redux